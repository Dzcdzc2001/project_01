const { requestGetData } = require("./__requestGetData__");
const { requestPostData } = require("./__requestPostData__");

const toData = {
    requestGetData,
    requestPostData
};

module.exports = toData;